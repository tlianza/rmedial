<h1>New Media Path</h1>
Enter a path <b>on the server</b> to search for media.

<%= error_messages_for :media_path %>

<% form_for(@media_path) do |f| %>
  <p>
    <label>On Filesystem:</label> <%= f.text_field 'filesystem_path'%> <br />
    <%= f.submit "Create" %>
  </p>
<% end %>

<%= link_to 'Back', {:controller=>:rmedial_settings, :action=>:index} %>
